pipeline {
    agent any

    stages {
        stage('Checkout Code') {
            steps {
                checkout scm
            }
        }

        stage('Build') {
            steps {
                echo "🔨 Building the application..."
                // Put your build command
                sh 'echo Build step executed'
            }
        }

        stage('Test') {
            steps {
                echo "🧪 Running tests..."
                // Put your test command
                sh 'echo Test step executed'
            }
        }

        stage('Deploy') {
            steps {
                echo "🚀 Deploying..."
                // Deploy commands go here
                sh 'echo Deployment executed'
            }
        }
    }

    post {
        success {
            echo "🎉 Pipeline executed successfully!"
        }
        failure {
            echo "❗ Pipeline failed!"
        }
    }
}

